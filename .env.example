# /fog (SF Fog Risk Map)
# Public Mapbox token — used by both the map renderer and the address geocoder.
# Get one at https://account.mapbox.com/access-tokens/
# In production, restrict the token to your domain(s).
NEXT_PUBLIC_MAPBOX_TOKEN=
